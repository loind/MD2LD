{
  "name": "md2ld",
  "version": "1.0.0",
  "description": "Push Markdown files to Lark Docs as a single binary",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "md2ld": "src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --compile --outfile dist/md2ld"
  },
  "dependencies": {
    "marked": "^12.0.0",
    "minimist": "^1.2.8"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/bun": "latest"
  }
}
